export type Tag = {
  id: number;
  name: string;
  bgColor: string;
  textColor: string;

  /**
   * timestamp default fields
   */
  createdAt?: string;
  /**
   * timestamp default fields
   */
  updatedAt?: string;
};
